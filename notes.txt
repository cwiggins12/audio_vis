cMake
ALSA(forces learning fundamentals), PipeWire("correct" for linux), PortAudio(too abstracted for learning, but cross plat) or miniaudio(happy medium) for audio capture
c++: audio analysis, link vulkan, audio_capture, windowing(SDL),
Vulkan: initially just for a basic spectrum analyser, peak meter, and rms meter
need Mesa's V3DV driver for pi

working order: get audio capture, get window, get vulkan init with static image, crank out some analysis functions, use the analysis output with vulkan

structure:
analyser/
	CMakeLists.txt
	include/
		miniaudio.h
		window.h
		audio_capture.h
		analysis.h
		renderer.h
	src/
		main.cpp
		v_graphics.???
		
TODO:
see whats up with nvim scopelines

Done:
audio capture with generic ring buffer :)
