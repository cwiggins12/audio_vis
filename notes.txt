TODO: 
need initial test spec, peak, and rms shader
need to handle gl buffers when output is over 6 kB
need another class to prepare audio outputs for rendering
also need a struct of info the renderer will need?
pass struct on shader change and let the class handle the changes?
That struct could also handle the initialization in the audio class

optional and cleanup before 1.0.0:
could rms and peak work in the callback by passing the static func to capture on init?
make list all devices functionality and allow for monitor substring to be var that can be replaced on cmdline?
give another command line function to check that vars will keep up with call back at 30 & 60 FPS? (or set fft_size according to fps?)
need Mesa's V3DV driver for this to work on a pi
trim all but what is used from miniaudio.h
hella comments

Done:
smoothing classes with logic for single, AoS, and SoA
audio parent class(covers analysis classes)
added logic in audio to use multiple configs of smoothValue arrays easily with more to come
this includes an arbitrary size version that will cubic interp up to an arbitrary freq, then non overlap rms the rest
Audio analysis(Peak, RMS, FFT classes)
Audio capture(wrapper of miniaudio.h)
windowing and events with sdl3
basic opengl context setup
