THIS WEEK:
hella documentation:
 - a full doc to explain each item in the spec and what the options do and outline the entire process of how a user can add and build shaders outside of using the spec (in progress)
 - github readme
 - gifs of some shaders
 - 1.0.0 gitHub release
 - demo video(have some footage of the pi running it too)
 - portfolio write up

pi target notes: need Mesa's V3DV driver, how will audio pass through happen?, check its device listings(should be the same standard linux devices),

Next week goals:
 - raw audio sample ssbo (would need to be hop size and can be stored as an array in bridge to pass to the ssbo easily)
 - mouseX, mouseY, and mouseDown uniforms
 - beat detection with a simple onset detector
 - more shaders!!!
 - allow for user defined fftSize and overlap percentage? allow for 0, 25, 50, 75 % overlaps, and allow for 10 - 13 fftOrder idk about allowing past 13. that'll 2x the ring buffer size gonna have to worry about pulling the windows fast enough based on device frame rate, so more error handling will be needed but nothing worse than the expression parser.
 - stb_truetype integration for user defined fonts
 - put hot reload notifictations on a bg thread with inotify/ReadDirectoryChangesW wrapper (profile this and make sure the context switches don't outweigh the savings). Also, try and get it to recognize a new directory being created and add it to presets array
 - windows build(AudioCapture and CMake will require the largest changes, but should be mostly painless. Famous last words. Also, make all the libs static during this process). REMINDER: during final profiling pass, look into speed diffs between clang and gcc (OTHER reminder: stb_image might only use SIMD on GCC when using the -msse2 flag)

FUTURE ADDITIONS:
 - Have 2 separate builds to allow for Linux and Windows users to not have to use GL ES. Def a lot of speed to gain from that on my end as well.
 - utility exes and a config.ini for device(audio & video) listing/choosing. maybe done through a dropdown menu of the audio_vis.exe?
 - timed shuffle mode (pressing a button opens a dialog box, user enters a time? or may want to wait to add until I have an application wide config in task 7)
 - look into a wayland fullscreen and window pos workaround, or if its necessary at all
 - More possible smoothing modes: Research for high end smoothing: precollation smoothing(super spency), weighted collation(wont do much without log bin grouping), log bin grouping with overlap(spency), or second smoothing pass on high end with the interp again
 - better sample rate changes(disallow resampling at any level and detect changes)(this would only be necessary or relevant for a user that needs the highest quality/speed, but is changing their hardware sample rate mid session. The analysis still works currently, but it analyzes resampled audio by either the OS internal audio processes(pipewire, pulse, winapi) or miniaudio in certain scenarios(alsa, certain windows configs))
 - trim all but what is used from miniaudio.h to chill out the binary size
 - Allow for user defined uniforms that could be adjusted by midi, key input, or ui
 - pre-built widgets/api for the graphically challenged (meters, mel-bin, etc.)
 - Allow export of current setup in standalone to VST3 with midi driven shader swaps. no JUCE, straight up steinbergSDK use.

Done This Week:
1. helper functions added to properly ensure all user input from spec input and expressions stay within type bounds and properly handle division cases as best as it can with an additional warning for the user when the result is truncated
2. shaders updated and mel-spec is looking good. Some smearing, but thats how it is at this fft size and overlap %
3. adjust error log prints in shader compile and link to subtract away the amount of lines from the fragment header
4. rebuilt peak rms metering to better fit the use case and vectorize more easily
5. replaced all ring buffer, capture, and analysis modulo math with a bit mask for speed
6. checked through path and string parsing for security. Added texture path saftey chacks and a throw in shader init
7. added destroyTextures calls on hot reload and started caching the error string
8. lowered amount of glFinish() calls (just calls once on swap now)
9. Optimized down to 7.5% of one core on my garbage laptop. Will be lower when I make the hot reload on a bg thread
10. organize headers into subdirectories
11. general shader fixes
12. feedback buffer size limit is now the lower of 4k framebuffer or hardware limit
13. feedback buffers may not work on devices that allow less than 6 bindings. Best I can think to do is just warn users in the log
14. 

OPTIMIZATION NOTES: the db conversions in av_bridge are hella pow() calls are rough instead it seems lookup tables could help. It would be the last big save to make that I can spot.
debug_build = cmake -S . -B debug_build -DCMAKE_BUILD_TYPE=Debug -DASAN=ON
profile_build = cmake -S . -B profile_build -DCMAKE_BUILD_TYPE=RelWithDebInfo
perf = perf record ./profile_build/audio_vis
perf report --stdio > perf_output.txt
memory usage checks: cat /proc/$(pgrep audio_vis)/smaps_rollup
watch -n 2 'grep -E "Rss|Pss:" /proc/$(pgrep audio_vis)/smaps_rollup'
Current usage: Total: 150 MB, Private: 69 MB, Shared: 83 MB. Pretty lean for what it does, imo

DOCUMENTATION NOTES: The only reason a shader will be skipped rather than present an error message will be if the frag.glsl or parent directory cannot be found or opened when switching to it.
current lines of code = 3730 (.hpp & .cpp only no .h or .c since they're external).

SHADER IDEAS: moving refractive glass over audio tools, bloom effects that line up with fft peaks from bottom left to top right, bubbles that go up from the freqs that are loudest and lift(could resue refraction to make them look cool and put a texture behind them), octave band analyzer(with old school vu meters as a moving radial or bar), waveform oscilloscope?(don't have the functionality for this yet. Its high up on the list though), a fractal zoom would be sick, particle field affected by FFT energy, audio-reactive raymarching to do cool stuff with sdfs, 
