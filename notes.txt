Tomorrow:
hot reload errors for spec changes need to be passed to shader for error display properly
Failed spec and shader compilation should fallback to compiling the error shader and displaying the error for hot reload!!!
need to handle sample rate change
need to handle displayhz change
finish specAssert logic in shader_loader(logical no nos that I have a warnig about in the spec comments)

Current phase:
7. do one more run of finalizing which low/high modes will be available
8. update shaders after all the fixes to use as examples for users
9. mel-spectrogram shader(audible bins usage), spec: isdb = off, peakrms = off, holds = off, no textures, useAudibleSize = true, isPerceptual = false, 
10. shader to test full bin output
11. shuffle button(when pressed, random next shader)
12. 7. test multi monitor setup

FINAL PHASE TO 1.0.0:
need hella more documentation in code and in repo for shader authors
proper security check with all of these cstrings from glsl and file pathing
refactor, profile, optimize, get it good as standalone for linux
test memory use: when program is running, in another console enter:
cat /proc/$(pgrep audio_vis)/smaps_rollup
look into speed diffs between clang and gcc (reminder: stb_image might only use SIMD on GCC when using the -msse2 flag)
need Mesa's V3DV driver to test on a pi

Possible future work:
stb_truetype integration for user defined fonts
look into a wayland fullscreen and window pos workaround, or if its necessary at all
timed shuffle mode(pressing a button opens a dialog box, user enters a time?)
put hot reload notifictations on a bg thread with inotify/ReadDirectoryChangesW wrapper
windows build(capture and cmake will require the largest changes, but should be mostly painless)
utility exes and a config.ini for device(audio & video) listing/choosing
trim all but what is used from miniaudio.h
dmx output for dmx based video walls(WOULD BE NIGHTMARE TO FIND DEVICE, make it up to users or DAWS in the case of a vst3?)
Allow for user defined uniforms that could be adjusted by midi, key input, or ui
pre-built widgets(meters, mel-bin, etc.)
Allow export of current setup in standalone to VST3 with midi driven shader swaps. no JUCE, straight up steinbergSDK use.

Done This Week:
1. Shader to test feedback buffer, bitmap font, and textures
2. made feedback buffer scale with width, height, or resolution
3. did the same for the customFFTSize
3. Cleaned up customFFTSize modes dramatically
4. fixed smooth value and bitmap text bugs
5. fft now has ability to ouput power, along with magnitude and decibels
6. Power, mag, or db setting can be configured in the spec now
7. added high end interpolation smoothing second pass strats. still needs testing
8. big ole 400 line refactor out of av_bridge
9. expression evaluation now works for the sizes and can use select variables that aren't known at compile time
10. HOPEFULLY finalized spec and spec parser aside from reworking the error handling and possibly trimming down some of the enum options
11. 

officially in second to last phase!!!!!
current loc(without spec.cfg's and frag.glsl's): 3711
