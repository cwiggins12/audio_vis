8. injected bit-mapped font
9. hot reloading
10. error display on errored hot reloaded shader(need bit-mapped font)
11. texture usage functionality (use stb_image, introspection, and make users label the textures to uniforms in spec)
12. look into different interpolation strategies for the low end. This cubic interp is rough

NEXT PHASE:
Make a few fun, abstract test shaders
Make sure shaders exist to test feedback buffer, hw/px/uv, bit-map font, and texture
try and pull off mel-spectrograms as well
handle height & width case in AudioSpec more robustly
have a percentage specific height and/or width case
maybe instead of listing flags for sizing, have a uint state?
finalize spec and injection after findings from the above shaders
when spec is finalized, finish specAssert logic in shader_loader.hpp
shuffle button(when pressed, random next shader)
timed shuffle mode(pressing a button opens a dialog box, user enters a time?)

FINAL PHASE:
look into a wayland fullscreen and window pos workaround
need Mesa's V3DV driver for this to work on a pi
trim all but what is used from miniaudio.h at the very end of development
need hella more documentation
security check with all of these cstrings from glsl and file pathing
refactor, profile, optimize, get it good as standalone for linux only (windows l8r)
test memory use: when program is running, in another console enter:
cat /proc/$(pgrep audio_vis)/smaps_rollup

Possible future work:
std_truetype integration for user defined fonts
everything working on a windows build(capture will require the largest changes)
utilities for device(audio & video) listing functions
allow for user to define audio device and video device some way, maybe a .ini or menu
Allow export of current setup in standalone to VST3, with midi that can drive shader changes
when you port to VST3: no JUCE, straight up steinbergSDK use. 
config utility to let users choose audio and video device and set it in a config.ini
user defined arrays in glsl for recursive buffers
texture handling in glsl
dmx output for dmx based video walls(WOULD BE NIGHTMARE TO FIND DEVICE, make it up to users, or DAWS in the case of a vst3?)
Allow for user defined uniforms that could be adjusted by midi, key input, or ui
pre-built widgets(meters, mel-bin, etc.)

Done This Week:
1. Extract the active shader into a ShaderPreset struct with hardcoded spec and the current test shader. Then verify. 
DONE
2. Add a std::vector<ShaderPreset> and an activeIdx int. Put two copies of the test preset in the list and wire the right arrow key to cycle activeIdx and call spec swap. Then verify.
DONE
3. Add a uniform struct that will be in each preset, each getting populated during use with the preset uniforms to hand to the user.
DONE
4. Write the injected header and the Shader constructor change that prepends it before compiling. Then verify.
DONE
5. Write the flat config file parser for AudioSpec. Point it at a hand-written file next to the test shader. Then verify.
DONE
6. Write the directory scanner that produces a vector<ShaderPreset> from a shaders/ folder. Move the test shader there, verify it loads and runs exactly as before.
DONE
7. Write a second real shader using only the injected helper functions. Drop it in shaders/. Verify cycling between the two works end to end including the spec swap and SSBO resize.
DONE
8. setup cmake to update build after each shader is updated. Need hot reloading next to not need to reconfigure the build every time a new shader is added.
DONE
9. Migration from SDL3 to glfw to make glad easier to deal with and remove the bloat from the binaries that will be made when I statically link the dependencies.
DONE
10. send all cerr and cout reporting to a log.txt file that is truncated each run.
DONE
11. ensure consistent lexicographic ordering of shaders in loader
DONE
12. full dynamic ssbo rework to a custom ssbo that is persistent, turning each frame's passes of data to the gpu into a couple of getters and a memcpy.
DONE
13. add left key to go back to prior shader
DONE
14. preset name display on window (glfwSetWindowTitle can do this on each swap)
DONE
15. Figure out full screen mode with glfw(start in device-sized, resizeable window. Allow non-windowed fullscreen on and off with a button press)
TESTS PLANNED IN PHASE 2
16. set up a feedback ssbo for the user to do what they would want with them
TESTS PLANNED IN PHASE 2
17. make the h/w pixel conventions forced through injection. Leave uv available for those that want it
TESTS PLANNED IN PHASE 2
