1.1 goals:
 - pure sample data ssbo DONE
 - ubo for all uniforms DONE
 - mouseX, mouseY, and mouseDown uniforms DONE
 - fft and hop size are now customizable by the user (but limited) DONE
 - stb_truetype integration for user defined fonts
 - dropdown menu to choose audio and video device
 - better sample rate/channel amount changes(disallow resampling at any level and detect changes and adapt to them)(this would only be necessary or relevant for a user that needs the highest quality/speed, but is changing their hardware sample rate or channel amount mid session. The analysis still works currently, but it analyzes resampled audio by either the OS internal audio processes(pipewire, pulse, winapi) or miniaudio in certain scenarios(alsa, certain windows configs)), may require new plumbing in miniaudio to just give me a callback when those config values change
 - raspberry pi support for all features
 - more shaders!!!

1.2 goals:
 - put hot reload notifictations on a bg thread with inotify/ReadDirectoryChangesW wrapper (profile this and make sure the context switches don't outweigh the savings). Also, try and get it to recognize a new directory being created and add it to presets array
 - windows build(AudioCapture and CMake will require the largest changes, but should be mostly painless. Famous last words. Also, make all the libs static during this process). REMINDER: during final profiling pass, look into speed diffs between clang and gcc (OTHER reminder: stb_image might only use SIMD on GCC when using the -msse2 flag)
 - timed shuffle mode (pressing a button opens a dialog box, user enters a time?)
 - More possible smoothing modes: Research for high end smoothing: precollation smoothing(super spency), weighted collation(wont do much without log bin grouping), log bin grouping with overlap(somewhat spency), or second smoothing pass on high end with the interp again
 - Allow for user defined uniforms that could be adjusted by midi, key input, or ui
 - Have 2 separate builds: one ES for pi and embedded devices, one not ES for Win and Lin?
 - VST3 port?
 - interpolation between presets on swap?
